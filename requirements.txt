# 播客解析项目依赖
requests>=2.28.0
pyyaml>=6.0
python-dotenv>=1.0.0
dashscope>=1.20.0  # 阿里云 DashScope SDK (Qwen ASR)
aiohttp>=3.9.0  # HTTP客户端（dashscope依赖）
websocket-client>=1.0.0  # WebSocket客户端（dashscope依赖）
openai>=1.0.0  # 可选，用于OpenAI API
anthropic>=0.3.0  # 可选，用于Anthropic API
